# Cliente HTTP (usado para robots.txt e checagem de redirects)
# Biblioteca que faz as requisições HTTP 
# (Baixa o HTML das páginas do G1)
requests>=2.31.0

# Parser HTML (usado em parser.py)
beautifulsoup4>=4.12.0  # Biblioteca que navega e extrai dados da árvore HTML
soupsieve>=2.5          # Biblioteca que dá suporte a seletores CSS dentro do BeautifulSoup

# Renderização de páginas com JavaScript (usado em client.py)
playwright>=1.40.0
greenlet>=3.1.0

# Testes
pytest>=8.0.0           # Framework de testes
pytest-cov>=4.1.0       # Plugin para cobertura de testes